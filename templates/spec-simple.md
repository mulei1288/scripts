# 脚本规范：[脚本名称]

> 简洁版规范模板 - 适用于简单工具脚本（<100行，单一功能）

## 基本信息

- **脚本名称**：`[script-name].sh`
- **严格性级别**：基础级 / 标准级 / 企业级
- **脚本类型**：可执行脚本 / 库文件 / 工具脚本
- **作者**：[作者名称]
- **创建日期**：YYYY-MM-DD
- **版本**：v1.0.0

## 功能描述

[用 1-3 句话描述脚本的核心功能]

示例：
- 这个脚本用于同步本地文件到远程服务器
- 支持增量同步和完整同步两种模式
- 自动处理文件冲突和错误重试

## 使用方法

### 基本语法

```bash
script-name.sh [选项] <参数>
```

### 使用示例

```bash
# 示例 1：基本用法
script-name.sh input.txt

# 示例 2：带选项
script-name.sh --verbose input.txt

# 示例 3：多个参数
script-name.sh -o output.txt data/
```

## 参数说明

### 必需参数

- `<参数1>`：参数描述
  - 类型：文件路径 / 目录路径 / 字符串 / 数字
  - 约束：必须存在 / 必须可读 / 必须可写

### 可选参数

- `-v, --verbose`：启用详细输出
- `-d, --debug`：启用调试模式
- `-o, --output <path>`：指定输出路径（默认：stdout）
- `-h, --help`：显示帮助信息

### 环境变量

- `DEBUG`：设置为 1 启用调试日志
- `VERBOSE`：设置为 1 启用详细输出

## 依赖工具

- bash 4.0+
- [其他必需工具，如 git, docker, jq 等]

## 输出说明

### 标准输出

[描述正常输出的内容和格式]

示例：
- 处理进度信息
- 处理结果摘要

### 标准错误

[描述错误输出的内容和格式]

示例：
- 错误消息：`[ERROR] 文件不存在: input.txt`
- 警告消息：`[WARN] 使用默认配置`

### 退出码

- `0`：成功
- `1`：一般错误
- `2`：参数错误
- `3`：依赖工具缺失

## 测试要点

- [ ] 正常流程测试：使用有效参数运行脚本
- [ ] 参数验证测试：测试缺失参数、无效参数
- [ ] 错误处理测试：测试文件不存在、权限不足等场景
- [ ] 边界条件测试：测试空输入、大文件等
- [ ] 环境变量测试：测试 DEBUG、VERBOSE 等环境变量

## 注意事项

- [重要提示 1]
- [重要提示 2]
- [已知限制]

示例：
- 脚本需要 root 权限运行
- 不支持 Windows 平台
- 大文件处理可能需要较长时间

---

**填写说明**：
1. 将所有 `[占位符]` 替换为实际内容
2. 删除不需要的章节或字段
3. 根据实际情况调整参数和示例
4. 确保所有信息准确、完整

